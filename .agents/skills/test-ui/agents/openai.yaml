interface:
  display_name: "Test UI"
  short_description: "Run recorded Aider console UI tests"
  default_prompt: "Use $test-ui to run the test cases in test/ui-test-plan.md, stop on the first failure, and show the console session."
